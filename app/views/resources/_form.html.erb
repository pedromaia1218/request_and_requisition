<%= form_with(model: resource) do |form| %>
  <% if resource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(resource.errors.count, "error") %> prohibited this resource from being saved:</h2>

      <ul>
        <% resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :Nome, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :Tipo, style: "display: block" %>
    <%= form.text_field :resource_type %>
  </div>

  <div>
    <%= form.label :Quantidade, style: "display: block" %>
    <%= form.number_field :amount %>
  </div>

  <div>
    <%= form.label :Disponível, style: "display: block" %>
    <%= form.check_box :availability %>
  </div>

  <div>
    <%= form.submit "Salvar recurso" %>
  </div>
<% end %>
